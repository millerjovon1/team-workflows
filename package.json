{
  "name": "team-workflows",
  "version": "1.0.0",
  "description": "Installable team workflow CLI for GitHub repos — branching, staging, production, and Netlify config",
  "type": "module",
  "bin": {
    "team-workflows": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "src/"
  ],
  "keywords": [
    "cli",
    "workflow",
    "git",
    "netlify",
    "vite",
    "staging",
    "production"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "audit": "npm audit",
    "security": "echo 'Security scan placeholder (add your tool)'",
    "ai:scan": "echo 'AI code scan placeholder (add your tool)'",
    "preview": "npx serve dist",
    "wf:preview": "npm run build && npm run preview"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
